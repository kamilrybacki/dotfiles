# i3 config — Everforest Cozy + Ditherpunk/D&D
# vim: set ft=i3config:

set $mod Mod4

# Colors from Xresources
set_from_resource $bg_dim  efBgDim  #272e33
set_from_resource $bg0     efBg0    #2e383c
set_from_resource $bg1     efBg1    #374145
set_from_resource $bg2     efBg2    #4a555b
set_from_resource $fg      efFg     #d3c6aa
set_from_resource $green   efGreen  #a7c080
set_from_resource $aqua    efAqua   #83c092
set_from_resource $blue    efBlue   #7fbbb3
set_from_resource $yellow  efYellow #dbbc7f
set_from_resource $orange  efOrange #e69875
set_from_resource $red     efRed    #e67e80
set_from_resource $purple  efPurple #d699b6

# Font
font pango:JetBrainsMono Nerd Font 9

# No title bars, 2px border
default_border pixel 2
default_floating_border pixel 2

# Gaps
gaps inner 10
gaps outer 5

# Disable focus follows mouse
focus_follows_mouse no

# Window colors
#                       border  backgr  text    indicator child_border
client.focused          $green  $bg0    $fg     $aqua     $green
client.focused_inactive $bg1    $bg0    $fg     $bg1      $bg1
client.unfocused        $bg1    $bg_dim $bg2    $bg1      $bg1
client.urgent           $red    $bg0    $fg     $red      $red
client.placeholder      $bg_dim $bg_dim $bg2    $bg_dim   $bg_dim
client.background       $bg0

# Keybindings — Launch
bindsym $mod+Return exec --no-startup-id kitty
bindsym $mod+d exec --no-startup-id rofi -show drun
bindsym $mod+Tab exec --no-startup-id rofi -show window
bindsym $mod+f exec --no-startup-id thunar
bindsym $mod+l exec --no-startup-id ~/.local/bin/lock.sh
bindsym Alt+1 exec --no-startup-id google-chrome
bindsym Print exec --no-startup-id xfce4-screenshooter

# Keybindings — Window management
bindsym $mod+Shift+q kill
floating_modifier $mod

bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+semicolon focus right

bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+semicolon move right

bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

bindsym $mod+b split h
bindsym $mod+v split v
bindsym $mod+Shift+f fullscreen toggle

bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

bindsym $mod+Shift+space floating toggle
bindsym $mod+space focus mode_toggle

# Workspaces
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# Volume (PulseAudio)
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +5% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -5% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# Resize mode
mode "resize" {
    bindsym h resize shrink width 5 px or 5 ppt
    bindsym j resize grow height 5 px or 5 ppt
    bindsym k resize shrink height 5 px or 5 ppt
    bindsym semicolon resize grow width 5 px or 5 ppt

    bindsym Left resize shrink width 5 px or 5 ppt
    bindsym Down resize grow height 5 px or 5 ppt
    bindsym Up resize shrink height 5 px or 5 ppt
    bindsym Right resize grow width 5 px or 5 ppt

    bindsym Return mode "default"
    bindsym Escape mode "default"
    bindsym $mod+r mode "default"
}
bindsym $mod+r mode "resize"

# Reload / restart
bindsym $mod+Shift+c reload
bindsym $mod+Shift+r restart
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'Exit i3?' -B 'Yes' 'i3-msg exit'"

# Autostart
exec --no-startup-id xrdb -merge ~/.Xresources
exec_always --no-startup-id ~/.config/polybar/launch.sh
exec --no-startup-id dunst
exec --no-startup-id feh --bg-fill ~/.local/share/wallpapers/current.png
exec --no-startup-id xss-lock --transfer-sleep-lock -- ~/.local/bin/lock.sh

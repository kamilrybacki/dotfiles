# Picom config

backend = "xrender";
vsync = true;

# Opacity
active-opacity = 1.0;
inactive-opacity = 0.75;
frame-opacity = 1.0;
inactive-opacity-override = false;

# Fade
fading = false;

# Shadows
shadow = false;

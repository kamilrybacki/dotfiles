---
name: hermes
when: working on the Hermes agent, its config, crons, skills, AGENTS.md, or the Paperclip agent company
paths: */Code/helm/charts/hermes*
---
- Never restart Hermes mid-task. Read-only exec/cp into the pod is fine.
- Hermes crons see ONLY `~/.hermes/.env` secrets. `hermes cron edit` has no `--model`/`--provider`.
- Hermes AGENTS.md is generated by rulesync from `dotfiles/.rulesync/`. Edit the source, run `npm run agents:sync`, then open a PR.
- Hermes reviews; Claude implements. Do not dispatch builds to Hermes.
- There is no Discord any more: Hermes is the Paperclip CEO (hermes_gateway -> api_server). Task it with a Paperclip issue assigned to `hermes`.

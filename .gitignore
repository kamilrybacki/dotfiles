# Secrets — never commit these
.zshrc.secrets
*.secrets

# Compiled / generated
*.pyc
__pycache__/

# Editor
.vscode/
*.swp
*.swo
.claude

# graft's local graph cache — regenerable, not committed (run `graft build`).
graft/
